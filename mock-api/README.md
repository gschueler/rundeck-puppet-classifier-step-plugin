implements a basic mock puppet classifier API server using Ratpack

# run:

    ./gradlew run

# test puppet classifier plugins

1. Add plugin to rundeck
2. configure baseUrl to be `http://localhost:5050`, any auth token
